@model MVCTest.workout1

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>workout1</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.workoutName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.workoutName)
            @Html.ValidationMessageFor(model => model.workoutName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.userName, "user")
        </div>
        <div class="editor-field">
            @Html.DropDownList("userName", String.Empty)
            @Html.ValidationMessageFor(model => model.userName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
